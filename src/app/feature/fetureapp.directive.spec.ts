import { FetureappDirective } from './fetureapp.directive';

describe('FetureappDirective', () => {
  it('should create an instance', () => {
    const directive = new FetureappDirective();
    expect(directive).toBeTruthy();
  });
});
